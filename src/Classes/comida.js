class comida {

    constructor(codigo, descricao, valor, principal) {
        this.codigo = codigo;
        this.descricao = descricao;
        this.valor = valor;
        this.principal = principal;
    }
}

export default comida