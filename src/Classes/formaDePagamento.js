class formaDePagamento{
    
    constructor(codigo, descricao, desconto, taxa){
        this.codigo = codigo;
        this.descricao = descricao;
        this.desconto = desconto;
        this.taxa = taxa;
    }
}

export default formaDePagamento;